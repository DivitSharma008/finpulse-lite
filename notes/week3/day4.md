# Day 4 — Trade Statistics & Report Generation

- Built a `trade_statistics()` function to compute win rate, average win/loss, and profit factor from the trade log.
- Created a `strategy_report()` function that writes all metrics to a formatted `.md` report file, saved under the `reports/` folder.
