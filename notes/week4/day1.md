# Day 1 — Pipeline & Refactoring

- Refactored the codebase and built a `main.py` pipeline that runs all five modules (`download_data`, `strategy_sma`, `backtest`, `metrics`, `report`) in sequence from a single entry point.
- Successfully generated and saved reports for 5 stocks using the pipeline.
